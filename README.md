# datastore-manager
a ModuleScript that handles saving and loading player data with an in-memory cache, auto-save loop, and retry logic. 

Edit defaultData() to define your fields. Call Load on join, Unload on leave, and use Get/Set/Increment anywhere on the server.

Use this as a modulescript in serverscriptstorage
refer to the usage example
store your own data
